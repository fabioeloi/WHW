# ADR 0008 — Program: WHW v0.1.0 (Program 001)

<!-- whw:program slug="whw-v0-1-0" waves="001-006" -->

- **Status:** Accepted
- **Date:** 2026-09-10
- **Waves:** 001–006 (6 waves)
- **Related:** WHY.md, [0001](0001-name-scope.md), [0005](0005-wave-contract.md)

> There is **no wave 007** in this program: extension requires a new charter ADR.

## Context

Build WHW itself with WHW: the v0.1.0 harness (CLI, SQL planning, gates,
roles, skills, docs, example, tests) developed in waves, dogfooded live, and
released MIT.

## Explicit exclusions

| Front | Reason | Revisit in |
| ----- | ------ | ---------- |
| `whw serve` (HTTP queue API) | v0.2 surface; needs auth design | Next program |
| Live PostgreSQL adapter (wired) | Schema ships; wiring is unproven need | On first request |
| CI plugins (Actions/other-CI snippets) | Generic template suffices for v0.1 | Next program |
| Metrics dashboard template | Snapshot + docs suffice | On demand |
| Importers (`whw migrate`) | No validated source formats yet | On demand |
| Translations beyond pt-BR | Cost without readers | On demand |

## Wave map

| Wave | Slug | ADR |
| ---- | ---- | --- |
| 001 | repo-scaffold | 0001 |
| 002 | sql-cli-core | 0005 |
| 003 | scaffolds-gates | 0006 |
| 004 | close-evaluate-run | 0004 |
| 005 | roles-skills-templates-docs | 0008 |
| 006 | program-close | 0008 |

Waves 001–005 build; 006 closes (inventory + metrics + retrospective).

## Execution

- Solo flow: atomic commits per letter with `(Wave NNN L)` trailers (no remote
  PRs until the public repo exists).
- `main` stays green: `node --test "tests/**/*.test.js"` + `whw gate run --tier pr` per letter.
- First wave doc-first (this charter + WHY/AGENTS + seeds); last wave closes.

## Close criteria

- [x] Every wave 001–006 closed via `whw close`
- [x] `whw gate run program-inventory` GO
- [x] `whw metrics --out .whw/metrics.json` recorded and linked below

## References

- `docs/plan.md` (wave log), `planning/` (seeds)

## Addendum Wave 005 — roles-skills-templates-docs

Shipped: 5 roles, 6 Agent Skills (agentskills.io-compliant), 18 templates,
full docs set (`why`/`how`/`what`, ADRs 0001–0008, glossary), `hello-wave`
example (walkthrough verified green), 34 `node:test` unit + e2e tests.
Evidence: `npm test` 34/34 pass; example close + 6 gates GO. Follow-ups: none.

## Addendum Wave 006 — program-close

Program 001 retrospective: 6 waves chartered and closed in one session;
WHW operated on itself throughout (queue, claims, evidence, gates, closes).
`program-inventory` GO (001–006 inside charter, no 007); metrics snapshot at
`.whw/metrics.json`. Exclusions from the charter stand for the next program
(`serve`, live PG wiring, CI plugins, dashboard, importers, translations).
Evidence: checkpoints + closes wave001-E … wave006-E. Follow-ups: v0.1.0 tag,
public repo proposal.
